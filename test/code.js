var should = require('should');
var sinon = require('sinon');

var MonotonicID = require('../');


var sandbox;

describe('montonic-id class', function() {
  beforeEach(function () {
    sandbox = sinon.sandbox.create();
  });

  afterEach(function () {
    sandbox.restore();
  });

  it('supports ...', function(done) {
    // todo
    done();
  });
});